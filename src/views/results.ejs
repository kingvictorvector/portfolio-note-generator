<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Generated Note</title>
    <link rel="stylesheet" href="/css/bootstrap.min.css">
    <style>
        .note-container {
            border: 1px solid #dee2e6;
            background-color: #f8f9fa;
            padding: 20px;
            border-radius: 5px;
            white-space: pre-wrap; /* This preserves line breaks */
            font-family: monospace;
        }
    </style>
</head>
<body>
    <div class="container mt-5">
        <h2>Generated Note</h2>
        <p>You can copy the text below.</p>
        <pre class="note-container" id="note-text"><%= noteContent %></pre>
        <textarea id="copyArea" style="position:absolute;left:-9999px;top:0;"><%= noteContent.replace(/</g, '&lt;').replace(/>/g, '&gt;') %></textarea>
        <button class="btn btn-primary mt-3" id="copy-btn">Copy to Clipboard</button>
        <a href="/upload" class="btn btn-secondary mt-3">Process Another PDF</a>
    </div>

    <script>
        document.getElementById('copy-btn').addEventListener('click', () => {
            const ta = document.getElementById('copyArea');
            ta.select();
            document.execCommand('copy');
            alert('Note copied to clipboard!');
        });
    </script>
</body>
</html> 